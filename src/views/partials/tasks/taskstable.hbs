<table>
  <thead>
    <tr>
      <th>N</th>
       <th>Titulo de la tarea</th>
       <th>Descripciones de la tarea</th>
       <th>Operaciones</th>
    </tr>   
  </thead>
    {{#each tasks}}
  <tbody>
    <tr>
      <td>{{@index}}</td>
      <td>{{title}}</td>
      <td>{{description}}</td>
       <td>
         {{#if done}}
            <a href="/done/{{_id}}"> hecho</a>
         {{else}}
             <a href="/done/{{_id}}">No realizado</a>
         {{/if}}
         <a href="/delete/{{_id}}">borrar</a>
          <a href="/task/{{_id}}/edit">actualizar</a>
       </td>
    </tr>
    {{/each}}
  </tbody>
</table>
 <!-- Para recorrre con handelerbar se utiliza
   Con for -> each
   -->